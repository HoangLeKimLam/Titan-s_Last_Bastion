"""_core — verbatim copy of Titan's Last Bastion core modules.

These files are mirrors of `Titan-s_Last_Bastion/core/*.py` so the prototype
can run standalone without importing the full game tree. When the real
game is finished, replace these copies with proper imports.
"""
